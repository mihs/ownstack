{
  "name": "deploy-test",
  "version": "1.0.0",
  "description": "This is a simple application used to test the deployment of a web server and a job server using ownstack",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./web.js"
  },
  "author": "mihs",
  "license": "MIT",
  "dependencies": {
    "express": "^4.14.0",
    "morgan": "^1.7.0"
  }
}
